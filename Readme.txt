## Installation
npm install cypress --save-dev

## Run tests

1. Run tests from cypress environment:
npx cypress open

Check Chrome browser and then click to spec.cy.js to run the test

2. Run test from terminal:
npx cypress run

## To break test click 'ctrl + c' in terminal or close emulated browser and environment.